<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-neweaten="{{eaten}}">
		{{#if eaten}}devoured{{else}}still whole{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
